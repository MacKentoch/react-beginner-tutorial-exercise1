import './index.css';
// import updateRecopy from './tools';

(function() {
  // reference to elements (input and <p> where we will recopy input value):
  const input = document.querySelector(/* COMPLETE_CODE_HERE */);
  const recopy = document.querySelector(/* COMPLETE_CODE_HERE */);

  // attach input event to <input /> element
  input.addEventListener('input', function(event) {
    // COMPLETE_CODE_HERE
    // console.log('VALUE:', YOUR_VALUE);
  });
})();
